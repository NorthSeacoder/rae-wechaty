import {Wechaty} from 'wechaty';
import config from '../common/constant/config';
const key = config.APIKEY;

export default async () => {
    // return new CronJob('0 9 * * *', async () => {

    // const bot = new Wechaty();
    // const rooms = await bot.Room.findAll({topic: 'bot'});
    // console.log({rooms});
    return 'test'
    // @ts-ignore
    //const {data:{newslist}} = await res.getEveryDayEn({params:{key}})

    //   for (const room of rooms) {
    //     // const alias = await room.
    //     for (const text of q) {
    //       if (room.owner().name().includes('山月')) {
    //         await room.say(text)
    //       }
    //     }
    //   }
    // }, null, true, 'Asia/Shanghai')
};
